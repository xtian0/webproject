<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fruit.dao.OrderItemDao">
    <insert id="saveOrderItem" parameterType="com.fruit.pojo.OrderItem">
        insert into `order_item`(name, count, price, total_price, order_id) values(#{name},#{count},#{price},#{totalPrice},#{orderId});
    </insert>
</mapper>